/**----------------------------------
 * Checking and securing options types.
 * Only for browsers.
 *----------------------------------*/
import { OptionsType } from "./types";
declare const filters: (options: OptionsType) => void;
export default filters;
